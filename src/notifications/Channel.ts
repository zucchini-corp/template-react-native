import {AndroidChannel} from '@notifee/react-native';

export type ChannelType = 'ch1' | 'ch2' | 'default';

const CH1: AndroidChannel = {
  id: '',
  name: '',
};

const CH2: AndroidChannel = {
  id: '',
  name: '',
};

const DEFAULT: AndroidChannel = {
  id: '',
  name: '',
};

export default {
  CH1,
  CH2,
  DEFAULT,
};
